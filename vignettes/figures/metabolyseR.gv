digraph metabolyseR {

  # a 'graph' statement
  graph [overlap = true, bgcolor = '#e7e7e7']

  # several 'node' statements
  node [style = filled,shape = box,fontname = Helvetica, fontsize = 14, fillcolor = white color = black]
  metabolyseR; preTreat; classification; featureSelection; correlations

  # several 'edge' statements
  metabolyseR -> {preTreat classification featureSelection correlations} 
}